services:
  norsk_tracker:
    image: nginx:1.27-alpine
    container_name: norsk_tracker
    restart: unless-stopped
    volumes:
      - ./site:/usr/share/nginx/html:ro
    networks:
      - infra

networks:
  infra:
    name: infra
    external: true
